import type { AboutExam } from "@/types/education/type.tests";

interface aboutProps {
    about : AboutExam
}

const DetTestAbout = ({about}:aboutProps) => {
    return (
        <div>
            
        </div>
    );
};

export default DetTestAbout;